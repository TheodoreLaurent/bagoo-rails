<div class="container">
  <div class="row">
    <div class="col-sm-4 col-sm-offset-4">
      <h1>Book</h1>
      <%= simple_form_for(@bag) do |f| %>
        <%= f.input :check_in %>
        <%= f.input :check_out %>
        <%= f.button :submit, class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
</div>
